# Firmware directories under /usr/lib/firmware pruned from the image (stage 50).
# One path per line, relative to /usr/lib/firmware. Lines starting with # ignored.
#
# Three classes of entry, on the same reasoning: hardware this image cannot be installed on,
# or — class 3 — a kernel driver the image will never load.
#
#   1. Server/datacenter NICs and storage — outside the desktop/laptop scope of plan/03.
#   2. ARM SoC *platform* firmware — DSP, modem, GPU-zap and boot blobs for phone, ARM-laptop,
#      Chromebook, automotive and IoT SoCs. This is an amd64-only image (PROFILE in
#      build.conf), so none of it can ever be requested by a driver here.
#   3. nouveau's per-GPU-generation firmware from linux-firmware. VIDEO_CARDS carries no
#      nouveau (plan/03) and x11-drivers/nvidia-drivers blacklists the kernel module
#      unconditionally (plan/10), so nothing on this image can ever modprobe nouveau to
#      request these blobs.
#
# CAREFUL with class 2: vendors ship laptop Wi-Fi/Bluetooth firmware alongside their ARM SoC
# firmware, and those DO load on x86. The rule used below is to prune only the directories
# named after an ARM *SoC*, never a directory named after a radio. Specifically:
#   - Qualcomm's x86-relevant radios live in ath10k/, ath11k/, ath12k/, qca/ and rtw89/ —
#     all separate top-level directories, none of them touched here.
#   - MediaTek's mt79xx are Wi-Fi parts (mt7925/mt7927 are the Wi-Fi 6E/7 M.2 modules found in
#     x86 laptops), so the whole mt79xx range is left alone; only the mt8xxx Chromebook/tablet
#     SoCs go.
# Getting this wrong produces a machine that boots with no network, which stage 70 cannot see
# (its QEMU guest is virtio and requests no firmware at all). When in doubt, keep the blob.

# ---- class 1: server/datacenter NICs and storage ----------------------------------
liquidio
netronome
mellanox
qed
bnx2x
cxgb3
cxgb4
ti-connectivity
mrvl/prestera
dpaa2

# ---- class 2: ARM SoC platform firmware -------------------------------------------
# Qualcomm Snapdragon, all of it: 458 MiB, the single largest prunable tree in the image and
# 28% of /usr/lib/firmware. Phone (sm8250, sdm845, apq8016/8096), ARM laptop (x1e80100,
# glymur, kaanapali), automotive/IoT (sa8775p, qcs6490, qrb4210, qcm2290, qdu100) and the
# venus/vpu ARM video cores. This entry replaces the narrower "qcom/sc8280xp" it grew out of —
# that was one ARM laptop SoC picked out of forty on exactly this reasoning.
#
# Two entries under here are arguable and go anyway: qcom/aic100 (5 MiB) is a PCIe AI
# accelerator that can sit in an x86 server, and qcom/venus-* is ARM video. Both are outside a
# desktop/laptop image's scope for the same reason the class-1 server NICs above are, and
# keeping them would mean enumerating ~40 subdirectories by hand and re-auditing that list on
# every linux-firmware bump.
qcom

# MediaTek ARM SoCs (Chromebooks, tablets) — 18 MiB. NOT mt79xx: see the warning above.
mediatek/mt8173
mediatek/mt8183
mediatek/mt8186
mediatek/mt8188
mediatek/mt8189
mediatek/mt8192
mediatek/mt8195
mediatek/mt8196

# ---- class 3: nouveau firmware (dead since nouveau was dropped, plan/10) ----------
# Every per-GPU-generation directory linux-firmware ships under nvidia/ — 154 MiB, verified
# against the actual linux-firmware-20260622 binpkg (`docker run ... immos-builder tar -tf`,
# not guessed). Kepler-mobile through Blackwell desktop, plus the Tegra SoC entries (already
# out of scope under class 2's amd64-only reasoning, listed here instead since they live under
# nvidia/ and the two reasons for dropping them are the same firmware bump apart). ga102 (98
# MiB) and tu102 (51 MiB) are the bulk of it. Four of the desktop entries below are actually
# symlinks to a sibling in the same family (ad103/104/106/107 -> ad102, gb102 -> gb100,
# gb203/205/206/207 -> gb202) but are listed explicitly rather than relying on that, since a
# future linux-firmware could turn any of them into a real directory without notice.
#
# NOT touched: nvidia/<version>/ (e.g. nvidia/595.91.07/gsp_*.bin), which is
# x11-drivers/nvidia-drivers' OWN firmware for the proprietary driver actually in use — a
# completely disjoint subtree (numeric dotted version vs. alpha chip codename, verified no
# overlap against both binpkgs). Do not add a bare "nvidia" entry above this comment: that
# would delete the proprietary driver's firmware along with nouveau's and break GPU
# acceleration outright.
nvidia/ad102
nvidia/ad103
nvidia/ad104
nvidia/ad106
nvidia/ad107
nvidia/ga100
nvidia/ga102
nvidia/ga103
nvidia/ga104
nvidia/ga106
nvidia/ga107
nvidia/gb100
nvidia/gb102
nvidia/gb202
nvidia/gb203
nvidia/gb205
nvidia/gb206
nvidia/gb207
nvidia/gh100
nvidia/gk20a
nvidia/gm200
nvidia/gm204
nvidia/gm206
nvidia/gm20b
nvidia/gp100
nvidia/gp102
nvidia/gp104
nvidia/gp106
nvidia/gp107
nvidia/gp108
nvidia/gp10b
nvidia/gv100
nvidia/tegra124
nvidia/tegra186
nvidia/tegra194
nvidia/tegra210
nvidia/tu102
nvidia/tu104
nvidia/tu106
nvidia/tu10x
nvidia/tu116
nvidia/tu117
