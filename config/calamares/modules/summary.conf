# The "this is what is about to happen" page (plan/16 §5.3, "Keep").
#
# It has no settings of its own — it renders each preceding viewmodule's prettyDescription().
# Present in the sequence because this installer's one action is a whole-disk erase, and the
# summary page is where the target disk is named one last time before it is rewritten.
---
