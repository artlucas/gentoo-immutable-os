# First-boot growth (plan/01/04): systemd-repart (in the initrd) extends the var
# partition to the end of whatever disk the image was written to; the filesystem
# itself is grown by x-systemd.growfs on the /var fstab entry.
[Partition]
Type=var
Weight=1000
