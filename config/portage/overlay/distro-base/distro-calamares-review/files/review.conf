# The packaged fallback for /etc/calamares/modules/review.conf. See the CMakeLists for why it
# exists at all when this page has nothing to configure: NO_CONFIG would stamp `noconfig: true`
# into module.desc, and setConfigurationMap() would then never be called for a page that one day
# has a key.
---
