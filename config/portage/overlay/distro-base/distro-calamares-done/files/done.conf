# The packaged fallback for /etc/calamares/modules/done.conf. The medium always gets the RENDERED
# copy; this exists so the module is loadable on its own, and so that a missing restartNowMode
# means "do not offer a restart" rather than an unparsed key.
---
restartNowMode:     never
restartNowCommand:  ""
